<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Lock-screen volume-chord trigger: a mediaPlayback foreground service holds a MediaSession
         so volume keys route to us while locked (Android 14 requires the typed permission). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.LT">

        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <!-- The full-screen landscape translator. Lock-screen flags are set now so a later
             volume-chord trigger can launch it straight over the keyguard. -->
        <activity
            android:name=".TranslateActivity"
            android:exported="true"
            android:screenOrientation="sensorLandscape"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.LT.Fullscreen">
            <!-- The launcher: tapping the icon (and Quick Tap "Open app") opens the translator. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Set as Digital Assistant -> hold power opens it (if you remap the power button). -->
            <intent-filter>
                <action android:name="android.intent.action.ASSIST" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Reliable lock-screen launch: swipe down on the lock screen, tap this tile. -->
        <service
            android:name=".TranslateTile"
            android:exported="true"
            android:icon="@drawable/ic_tile"
            android:label="Translate"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Shelved volume-chord service (kept for a future revisit; no longer started). -->
        <service
            android:name=".ChordService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
    </application>
</manifest>
